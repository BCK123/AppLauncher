<Window x:Class="AppLauncher.Views.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="设置" Height="200" Width="520" WindowStartupLocation="CenterOwner">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Grid.Row="0" VerticalAlignment="Center">
            <TextBlock Text="存储目录：" VerticalAlignment="Center" Margin="0,0,8,0"/>
            <TextBox x:Name="TxtDir" Width="360" VerticalAlignment="Center"/>
            <Button Content="浏览..." Click="BtnBrowse_Click" Margin="8,0,0,0"/>
        </StackPanel>

        <TextBlock Grid.Row="1" Margin="0,12,0,0" Foreground="Gray" Text="留空使用默认 %APPDATA%\\AppLauncher" />

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="取消" Width="80" Margin="0,0,8,0" Click="BtnCancel_Click"/>
            <Button Content="保存" Width="80" Click="BtnSave_Click"/>
        </StackPanel>
    </Grid>
</Window>
