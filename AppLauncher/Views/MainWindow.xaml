<Window
    x:Class="AppLauncher.Views.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:HandyControl.Controls;assembly=HandyControl"
    xmlns:converters="clr-namespace:AppLauncher.Converters"
    xmlns:prism="http://prismlibrary.com/"
    Title="AppLauncher"
    Width="420"
    Height="520"
    MinWidth="360"
    MinHeight="580"
    prism:ViewModelLocator.AutoWireViewModel="True">

    <Window.Resources>
        <ResourceDictionary>
            <converters:IconPathToImageConverter x:Key="IconPathToImageConverter" />
            <converters:VisibilityConverter x:Key="VisibilityConverter" />
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Resources/Styles.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <!--  主容器：不设高度  -->
    <DockPanel LastChildFill="True">

        <!--  顶部栏  -->
        <Grid
            Height="60"
            Background="White"
            DockPanel.Dock="Top">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBlock
                Grid.Column="0"
                Margin="12,0"
                VerticalAlignment="Center"
                Style="{StaticResource TopTitleTextStyle}"
                Text="AppLauncher" />

            <StackPanel
                Grid.Column="2"
                Margin="0,0,12,0"
                VerticalAlignment="Center"
                Orientation="Horizontal">

                <Button Command="{Binding ShowShortcutCommand}" Content="快捷方式" />
                <Button Command="{Binding ShowVisionCommand}" Content="新功能" />

                <Button
                    Click="BtnAdd_Click"
                    Style="{StaticResource IconButtonStyle}"
                    ToolTip="添加">
                    <Image
                        Width="18"
                        Height="18"
                        Source="/Resources/icons/add.png" />
                </Button>

                <Button
                    Command="{Binding OpenSettingsCommand}"
                    Style="{StaticResource IconButtonStyle}"
                    ToolTip="设置">
                    <Image
                        Width="18"
                        Height="18"
                        Source="/Resources/icons/setting.png" />
                </Button>
            </StackPanel>
        </Grid>

        <!--  底部状态栏  -->
        <Grid
            Height="60"
            Background="White"
            DockPanel.Dock="Bottom">
            <StackPanel
                Margin="12,0"
                VerticalAlignment="Center"
                Orientation="Horizontal">
                <TextBlock Text="CPU：" />
                <TextBlock Text="{Binding Monitor.CpuUsage, StringFormat={}{0:F2}%}" />
            </StackPanel>
        </Grid>

        <!--  中间主内容区：自动填充  -->
        <Grid Background="#F5F5F5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="120*" />
                <ColumnDefinition Width="41*" />
                <ColumnDefinition Width="49*" />
            </Grid.ColumnDefinitions>
            <ContentControl Grid.ColumnSpan="3" prism:RegionManager.RegionName="MainRegion" />
        </Grid>

    </DockPanel>
</Window>
